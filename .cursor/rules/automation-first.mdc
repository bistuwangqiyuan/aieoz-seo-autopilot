---
description: 全自动化交付——所有环节必须由软件与 AI 无人化完成
alwaysApply: true
---

# 全自动化原则

团队没有全职人力，**所有工作必须通过软件和 AI 完成**。任何方案的每个环节都要是自动化的、AI 完成的、无人化的、软件化的。

## 硬性要求

- 交付物必须是**可自动运行的代码/脚本/工作流**，不是需要人去执行的操作手册。
- 任何步骤如果依赖「人工检查」「人工填写」「人工上传」，视为方案未完成，必须改为程序化实现或 AI 代理实现。
- 定时、触发、重试、告警、日志都要内建，做到无人值守也能持续运行。
- 唯一允许保留给人的，只有第七条要求的**最终决策权**（价值判断与重大裁决），不包括执行劳动。

## 判定示例

```text
❌ 差：「导出关键词数据，人工筛选后填入表格」
✅ 好：scripts/fetch_keywords.py 定时拉取 → 规则+LLM 打分自动筛选 → 写入数据库 → 失败自动重试并告警

❌ 差：「每周检查一次收录情况」
✅ 好：GitHub Actions cron 每周执行 check_indexing.py，产出报告并自动提交/推送通知
```

## 自检

给出任何方案前自问：这一步如果没有人，能不能自己跑完？不能，就重新设计。
